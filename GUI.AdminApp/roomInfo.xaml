<Page
    x:Class="GUI.AdminApp.roomInfo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GUI.AdminApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Margin="30 30 0 0" HorizontalAlignment="Center">
            <TextBlock FontSize="20" Margin="0 0 0 10" x:Name="RoomDoorNumber"/>
            <TextBlock FontSize="20" Margin="0 0 0 10" x:Name="RoomId"/>
            <TextBlock FontSize="20" Margin="0 0 0 10" x:Name="RoomCapacityId"/>
            <TextBlock FontSize="20" Margin="0 0 0 10" x:Name="RoomCategoryId"/>
            <TextBlock FontSize="20" Margin="0 0 0 10" x:Name="RoomPrice"/>
            <TextBlock FontSize="20" Margin="0 0 0 10" x:Name="RoomStatus"/>
            <Button x:Name="goBack" Click="goBack_Click">Back</Button>
        </StackPanel>
    </Grid>
</Page>
