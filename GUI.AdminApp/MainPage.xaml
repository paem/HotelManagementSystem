<Page
    x:Class="GUI.AdminApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GUI.AdminApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


        <ListBox x:Name="userList" Margin="0 200 0 0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Height = "35" Width = "400"  Margin="200 0 0 0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding CustomerFName}" />
                            <TextBlock Text="{Binding CustomerLName}"  />
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <SearchBox x:Name = "mySearchBox"
         FocusOnKeyboardInput = "False"
         QuerySubmitted= "mySearchBox_QuerySubmitted" Height = "35" Width = "400" Margin = "10,67,-50,538"
                   />
        <!-- <AutoSuggestBox x:Name="SearchBox" PlaceholderText="Search" QueryIcon="Find" Width="200"
                TextChanged="AutoSuggestBox_TextChanged"
                QuerySubmitted="AutoSuggestBox_QuerySubmitted"
                SuggestionChosen="AutoSuggestBox_SuggestionChosen"/>
-->
    </Grid>
</Page>
