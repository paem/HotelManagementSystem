<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GUI.AdminApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:HMSServiceReference="using:GUI.AdminApp.HMSServiceReference"
    x:Class="GUI.AdminApp.MainPage"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <ListView x:Name="userList" Margin="0,65,0,-65" SelectionChanged="userList_SelectionChanged" > <!--ListBox kan bytas ut mot GirdView-->
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Height = "35" Width = "400"  Margin="200 0 0 0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="txbName" Text="{Binding CustomerFName}" />
                            <TextBlock x:Name="txbLName" Text="{Binding CustomerLName}"  />
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
            <TextBlock Height="100" TextWrapping="Wrap" Text="TextBlock" Width="100"/>
        </ListView>

        <AutoSuggestBox x:Name="AutoSuggBox" Header="Customer Search" PlaceholderText="Search for customer" 
                        QuerySubmitted="AutoSuggBox_QuerySubmitted" 
                        SuggestionChosen="AutoSuggBox_SuggestionChosen" 
                        TextChanged="AutoSuggBox_TextChanged"
                        TextMemberPath="name" 
                        DisplayMemberPath="id" />
    </Grid>
</Page>